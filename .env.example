# ── Supabase ────────────────────────────────────────────────────────────────
# From: Supabase dashboard → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key

# SERVER-ONLY. Never prefix with NEXT_PUBLIC_. Never import into a client component.
# Lives only in lib/supabase/admin.ts, route handlers, and Edge Functions.
SUPABASE_SERVICE_ROLE_KEY=your-service-role-secret-key

# ── Google Maps (venue picking only — added in M6) ───────────────────────────
# Browser key, restricted to Maps JS + Places, HTTP-referrer locked to your domain
NEXT_PUBLIC_GOOGLE_MAPS_KEY=your-browser-maps-key
# Server key for the /api/place-photo proxy (keeps the key off the client)
GOOGLE_MAPS_SERVER_KEY=your-server-maps-key

# ── Email (M2 — pick one) ────────────────────────────────────────────────────
# Resend for transactional email; leave blank to use Supabase Auth's provider
RESEND_API_KEY=

# ── App ──────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_SITE_URL=http://localhost:3000
